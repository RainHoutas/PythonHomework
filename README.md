# Python程序设计作业

本仓库包含Python程序设计练习的解决方案。以下是作业列表及每个练习的简要说明。

---

## 作业一
**本作业主要练习以下知识点:** 函数定义, float类型转换, input(), print(), f-string, 算术运算符 (**), 算术运算符, import math, math.pi, 元组返回

- **第一题.py (Exercise 1.py)**
  - 将摄氏温度转换为开氏温度。
  - 知识点: 函数定义, float类型转换, input(), print(), f-string
- **第二题.py (Exercise 2.py)**
  - 根据体重和身高计算身体质量指数 (BMI)。
  - 知识点: 函数定义, float类型转换, input(), print(), f-string, 算术运算符 (**)
- **第三题.py (Exercise 3.py)**
  - 根据圆的半径计算其直径和面积。
  - 知识点: import math, 函数定义, float类型转换, input(), print(), f-string, math.pi, 元组返回
- **第四题.py (Exercise 4.py)**
  - 计算在使用4吨卡车运输若干次后，剩余煤炭需要用2.5吨卡车额外运输多少次。
  - 知识点: 函数定义, float类型转换, print(), f-string, 算术运算符

---

## 作业二
**本作业主要练习以下知识点:** import turtle, 函数定义, turtle库基本绘图 (颜色, 填充, forward, left, done, circle, right, penup, goto, pendown)

- **第一题.py (Exercise 1.py)**
  - 使用turtle图形库绘制一个填充的蓝色等边三角形。
  - 知识点: import turtle, 函数定义, turtle库基本绘图 (颜色, 填充, forward, left, done)
- **第二题.py (Exercise 2.py)**
  - 使用turtle图形库绘制一个红色的心形，并将其向上移动。
  - 知识点: import turtle, 函数定义, turtle库基本绘图 (颜色, 填充, left, forward, circle, right, penup, goto, pendown, done)

---

## 作业三
**本作业主要练习以下知识点:** import turtle, 函数定义, turtle库基本绘图 (同作业二/第二题), 字符串切片 (反转), input(), 字符串方法 (upper, lower, title, find, count, replace, strip, split), f-string, len(), 字符串条件判断 (in)

- **第一题.py (Exercise 1.py)**
  - 使用turtle图形库绘制一个红色的心形，并将其向上移动。(类似于作业二的第二题)
  - 知识点: import turtle, 函数定义, turtle库基本绘图 (同作业二/第二题)
- **第二题.py (Exercise 2.py)**
  - 反转给定的字符串 "Python"。
  - 知识点: 函数定义, 字符串切片 (反转)
- **第三题.py (Exercise 3.py)**
  - 输入一个英文名字，并以大写、小写和标题大小写形式打印。
  - 知识点: input(), 字符串方法 (upper, lower, title)
- **第四题.py (Exercise 4.py)**
  - 对给定字符串执行多种操作：查找'Luoyang'的索引，统计出现次数，将'000'替换为空格，转换为大写，去除首尾空格，以及分割字符串。
  - 知识点: 字符串方法 (find, count, replace, upper, strip, split), f-string
- **第五题.py (Exercise 5.py)**
  - 计算输入字符串中的单词数量。
  - 知识点: input(), 字符串方法 (split), len()
- **第六题.py (Exercise 6.py)**
  - 在字符串 "Life is short. I use python" 中将 "python" 替换为 "Python"。
  - 知识点: 字符串条件判断 (in), 字符串方法 (replace)

---

## 作业四
**本作业主要练习以下知识点:** 函数定义, float类型转换, input(), print(), f-string, if-elif-else 条件语句, 逻辑运算符 (and, or), 算术运算符 (**), 算术运算符, 字符串索引, 字符串方法 (isdigit, isalpha, upper, isspace), len(), 类型转换 (int), for循环, for循环 (range), 元组返回, 嵌套for循环 (range), print() (end参数), while循环, break, import random, random.randint()

- **第一题.py (Exercise 1.py)**
  - 根据不同的工资范围计算党费。
  - 知识点: 函数定义, float类型转换, input(), print(), f-string, if-elif-else 条件语句
- **第二题.py (Exercise 2.py)**
  - 根据三角形的三边长度判断其类型（等边、等腰、直角、普通三角形）。
  - 知识点: 函数定义, float类型转换, input(), print(), if-elif-else 条件语句, 逻辑运算符 (and, or), 算术运算符 (**)
- **第三题.py (Exercise 3.py)**
  - 根据输入的座位号（例如 "10A"）判断其是靠窗、过道还是中间座位。
  - 知识点: 函数定义, input(), print(), if-elif-else 条件语句, 字符串索引, 字符串方法 (isdigit, isalpha, upper), len(), 类型转换 (int)
- **第四题.py (Exercise 4.py)**
  - 统计输入字符串中字母、空格、数字和其他字符的数量。
  - 知识点: 函数定义, input(), print(), f-string, for循环, 字符串方法 (isalpha, isspace, isdigit)
- **第五题.py (Exercise 5.py)**
  - 解决经典的鸡兔同笼问题（根据总头数和总脚数计算鸡和兔的数量）。
  - 知识点: 函数定义, int类型转换, input(), print(), f-string, for循环 (range), if条件语句, 元组返回
- **第六题.py (Exercise 6.py)**
  - 计算序列和 S = a + aa + aaa + ... （例如，若输入为3，则和为 1 + 12 + 123）。若输入大于等于10则报错。
  - 知识点: 函数定义, int类型转换, input(), print(), if条件语句, for循环 (range), 算术运算符
- **第七题.py (Exercise 7.py)**
  - 打印九九乘法表。
  - 知识点: 嵌套for循环 (range), print() (end参数), f-string
- **第八题.py (Exercise 8.py)**
  - 模拟一个简单的中国移动 (10086) 客户服务菜单，用于查询余额、流量、通话时间或退出。
  - 知识点: 函数定义, while循环, input(), print(), if-elif-else 条件语句, break
- **第九题.py (Exercise 9.py)**
  - 一个简单的猜数字游戏，用户尝试猜测一个0到10之间随机生成的数字。
  - 知识点: import random, 函数定义, random.randint(), while循环, int类型转换, input(), print(), f-string, if-elif-else 条件语句, break

---

## 作业五
**本作业主要练习以下知识点:** 列表, 函数定义, set转换去重, len(), if-else 条件语句, 列表合并 (+), sorted()函数 (reverse参数), 列表推导式, if-else 条件 (列表推导式内), 算术运算符 (%), import random, import string, for循环 (range), random.choice(), string.ascii_letters, string.digits, 列表方法 (append, remove, 修改元素, insert), 列表创建, 列表复制 (切片[:]), zip()函数, 列表推导式 (创建元组列表), 元组, 列表 (元组内嵌列表), while循环, input(), print(), 字符串方法 (startswith), next() (生成器表达式与默认值)

- **第一题.py (Exercise 1.py)**
  - 检查给定的数字列表中是否包含重复元素。
  - 知识点: 列表, 函数定义, set转换去重, len(), if-else 条件语句
- **第二题.py (Exercise 2.py)**
  - 合并两个数字列表，并将合并后的列表按降序排序。
  - 知识点: 列表合并 (+), sorted()函数 (reverse参数)
- **第三题.py (Exercise 3.py)**
  - 转换一个数字列表，将偶数平方，奇数保持不变。
  - 知识点: 列表推导式, if-else 条件 (列表推导式内), 算术运算符 (%)
- **第四题.py (Exercise 4.py)**
  - 生成一个由字母和数字组成的6位随机验证码。
  - 知识点: import random, import string, 列表, for循环 (range), random.choice(), string.ascii_letters, string.digits, 列表方法 (append), 字符串方法 (join)
- **第五题.py (Exercise 5.py)**
  - 通过管理自己和朋友最喜欢的食物列表来演示列表的复制和修改。
  - 知识点: 列表创建, 列表复制 (切片[:]), 列表方法 (append, remove, 修改元素)
- **第六题.py (Exercise 6.py)**
  - 使用 `zip` 和列表推导式创建一个元组列表，每个元组包含一个食物项及其对应的卡路里计数。
  - 知识点: zip()函数, 列表推导式 (创建元组列表)
- **第七题.py (Exercise 7.py)**
  - 修改元组内部列表中的元素。
  - 知识点: 元组, 列表 (元组内嵌列表), 列表方法 (append, 修改元组内列表元素)
- **第八题.py (Exercise 8.py)**
  - 模拟一个简单的购物系统，用户可以输入商品信息，通过商品编号将商品添加到购物车，并查看购物车内容。
  - 知识点: 列表, while循环, input(), print(), 字符串方法 (startswith), next() (生成器表达式与默认值), 列表方法 (insert)

---

## 作业6
**本作业主要练习以下知识点:** 字典创建, 字典遍历 (keys, values, items), 字典添加/修改元素, 字典方法 (clear), 字典, int类型转换, input(), for循环, sorted() (字典键排序), 字符串方法 (split, strip), 列表推导式, if-in 条件 (字典键检查), import random, random.randint(), set去重, sorted(), 列表嵌套字典, print(), next() (查找字典列表), f-string, 集合, 元组, 集合方法 (add), 集合推导式, len(), max(), min(), 集合运算 (| union, & intersection, - difference), import jieba, from collections import Counter, jieba.lcut (分词), 列表推导式 (过滤), collections.Counter(), most_common()方法

- **第一题.py (Exercise 1.py)**
  - 演示各种字典操作：遍历键、值和键值对；添加新的键值对；修改现有的键值对；以及清空字典。
  - 知识点: 字典创建, 字典遍历 (keys, values, items), 字典添加/修改元素, 字典方法 (clear)
- **第二题.py (Exercise 2.py)**
  - 输入N个学生的信息（学号和姓名），将它们存储在字典中，然后按学号排序打印学生信息。
  - 知识点: 字典, int类型转换, input(), for循环, sorted() (字典键排序)
- **第三题.py (Exercise 3.py)**
  - 根据用户输入创建一个“喜欢的事物”字典，其中键是类别（例如食物、宠物），值是该类别中喜欢项目的列表。
  - 知识点: 字典, int类型转换, input(), for循环, 字符串方法 (split, strip), 列表推导式
- **第四题.py (Exercise 4.py)**
  - 统计给定字符串中每个字符出现的次数。
  - 知识点: 字典, for循环, if-in 条件 (字典键检查)
- **第五题.py (Exercise 5.py)**
  - 生成N个随机整数（N <= 1000，数字范围1到1000），然后打印排序后的唯一数字列表（称为“学生ID”）。
  - 知识点: import random, int类型转换, input(), 列表推导式, random.randint(), set去重, sorted()
- **第六题.py (Exercise 6.py)**
  - 模拟一个基本的火车票预订系统。显示可用车次，允许用户按车次编号选择火车，并确认预订。
  - 知识点: 列表嵌套字典, for循环, print(), input(), next() (查找字典列表), f-string
- **第七题.py (Exercise 7.py)**
  - 输入5位朋友的姓名和电话号码，将它们作为元组存储在集合中，然后打印联系人列表。
  - 知识点: 集合, for循环, input(), 元组, 集合方法 (add)
- **第八题.py (Exercise 8.py)**
  - 创建两个集合A和B，其中包含10个随机整数（0-10）。打印这两个集合，它们的长度，最大/最小值，以及并集、交集和差集（A-B）。
  - 知识点: import random, 集合推导式, random.randint(), print(), len(), max(), min(), 集合运算 (| union, & intersection, - difference)
- **第九题.py (Exercise 9.py)**
  - 使用 `jieba` 库对示例文本（朱自清的《春》）进行中文分词，并使用 `collections.Counter` 找出并打印出现频率最高的前10个词语（长度 > 1）。
  - 知识点: import jieba, from collections import Counter, jieba.lcut (分词), 列表推导式 (过滤), collections.Counter(), most_common()方法

---

## 作业⑦
**本作业主要练习以下知识点:** 函数定义, for循环 (字符串遍历), int类型转换, 算术运算符 (%), if-else 条件语句, print(), f-string, 字典, 函数参数解包 (**), 可变参数 (*args), for循环, 算术运算符, len(), 列表 (权重), 字符串索引, for循环 (range), try-except (ValueError), 字符串方法 (upper)

- **1.py**
  - 计算数字字符串（"123456789"）中奇数位和偶数位的和。
  - 知识点: 函数定义, for循环 (字符串遍历), int类型转换, 算术运算符 (%), if-else 条件语句, print(), f-string
- **2.py**
  - 定义一个打印景点信息的函数，并使用龙门石窟的详细信息调用该函数（其中“河男省”应为“河南省”，“与”应为“豫”）。
  - 知识点: 函数定义, 字典, print(), f-string, 函数参数解包 (**)
- **3.py**
  - 定义一个函数 `multiply_all`，它接受可变数量的参数 (`*args`) 并返回它们的乘积。
  - 知识点: 函数定义, 可变参数 (*args), for循环, 算术运算符
- **4.py**
  - 通过检查长度和校验码来验证中国国民身份证号码。如果有效，则提取并打印出生日期和性别。
  - 知识点: 函数定义, len(), 列表 (权重), 字符串索引, for循环 (range), try-except (ValueError), int类型转换, 算术运算符 (%), if-else 条件语句, print(), f-string, 字符串方法 (upper)

---

## 作业8
**本作业主要练习以下知识点:** 函数定义 (递归), if-elif-else 条件语句, raise ValueError, try-except (ValueError, RecursionError), int类型转换, input(), print(), f-string, 函数定义, 字典 (嵌套字典), while循环, break, float类型转换, 列表, `if __name__ == "__main__":`, 列表 (存储字典), for循环 (enumerate), 列表方法 (append, pop), 字符串方法 (lower)

- **1.py**
  - 使用递归计算第N个斐波那契数，包含输入验证和递归深度错误处理。
  - 知识点: 函数定义 (递归), if-elif-else 条件语句, raise ValueError, try-except (ValueError, RecursionError), int类型转换, input(), print(), f-string
- **2.py**
  - 模拟饮料自动售货机：显示饮料，允许选择，处理支付（模拟投币/二维码），并出货。
  - 知识点: 函数定义, 字典 (嵌套字典), print(), f-string, while循环, input(), if-elif-else 条件语句, break, float类型转换, try-except (ValueError), 列表, `if __name__ == "__main__":`
- **3.py**
  - 实现一个简单的学生管理系统（添加、删除、修改、查询学生ID、姓名、年龄）。数据存储在字典列表中。
  - 知识点: 列表 (存储字典), 函数定义, print(), input(), for循环 (enumerate), if-elif-else 条件语句, break, int类型转换, try-except (ValueError), 列表方法 (append, pop), 字符串方法 (lower), `if __name__ == "__main__":`

---

## 作业九
**本作业主要练习以下知识点:** import math, class定义, __init__构造方法, 实例属性, 实例方法 (self), math.pi, print(), f-string, 类属性, 私有属性 (__), from datetime import datetime, date, try-except (ValueError), datetime.strptime(), date(), date.today(), if条件语句

- **1.py**
  - 定义一个 `Circle` 类，包含计算其周长和面积的方法。
  - 知识点: import math, class定义, __init__构造方法, 实例属性, 实例方法 (self), math.pi, print(), f-string
- **2.py**
  - 定义一个 `House` 类，使用类属性来计算创建的房屋实例总数，并计算房屋的面积。
  - 知识点: class定义, 类属性, __init__构造方法, 实例属性, 实例方法 (self), print(), f-string
- **3.py**
  - 定义一个 `Course` 类，包含课程编号、名称、教师以及一个私有属性表示上课地点。包含一个显示课程信息的方法。
  - 知识点: class定义, __init__构造方法, 实例属性, 私有属性 (__), 实例方法 (self), print(), f-string
- **4.py**
  - 定义一个 `Medicine` 类，包含私有属性：价格、生产日期和失效日期。包含获取这些详细信息、计算保质期和检查是否过期的方法。处理日期字符串解析。
  - 知识点: from datetime import datetime, date, class定义, __init__构造方法, 私有属性 (__), try-except (ValueError), datetime.strptime(), date(), date.today(), 实例方法 (self), if条件语句, print(), f-string

---

## 作业十
**本作业主要练习以下知识点:** import math, class定义, __init__构造方法, 实例属性, 实例方法 (self), math.pi, print(), f-string (同 作业九/1.py), 操作符重载 (__add__, __str__), isinstance(), NotImplemented, for循环, len(), if条件语句

- **1.py**
  - 定义一个 `Circle` 类，包含计算其周长和面积的方法。(与 作业九/1.py 相同)
  - 知识点: import math, class定义, __init__构造方法, 实例属性, 实例方法 (self), math.pi, print(), f-string (同 作业九/1.py)
- **2.py**
  - 定义一个 `Student` 类，重载 `+` 运算符以计算学生成绩总和，重载 `__str__` 以打印信息。计算多个学生的总分和平均分。
  - 知识点: class定义, __init__构造方法, 实例属性, 操作符重载 (__add__, __str__), isinstance(), NotImplemented, for循环, len(), print(), f-string
- **3.py**
  - 使用 `Fridge` 和 `Elephant` 类实现“把大象放进冰箱”的问题，并模拟其交互过程。
  - 知识点: class定义, __init__构造方法, 实例属性, 实例方法 (self), if条件语句, print(), f-string

---

## 作业十一
**本作业主要练习以下知识点:** 函数定义 (生成器函数, yield), isinstance(), raise TypeError, 字符串方法 (split, isalpha, title), filter(), ''.join(), for循环, try-except (TypeError), print(), f-string, 列表, 列表推导式, sorted()

- **1.py**
  - 定义一个生成器函数 `get_long_words`，用于从输入字符串中提取长度超过4个字符的单词（在清理掉非字母字符后）。
  - 知识点: 函数定义 (生成器函数, yield), isinstance(), raise TypeError, 字符串方法 (split, isalpha), filter(), ''.join(), for循环, try-except (TypeError), print(), f-string
- **2.py**
  - 将字符串列表转换为标题大小写（首字母大写），然后按字母顺序对列表进行排序。
  - 知识点: 列表, 列表推导式, 字符串方法 (title), sorted(), print(), f-string
